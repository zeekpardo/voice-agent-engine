{
  "name": "agent-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Media-plane worker for the voice agent engine: runs on LiveKit Cloud (lk agent deploy), assembles an AgentSession per dispatch from gateway-stored agent configs, executes tools-as-webhooks, and reports transcripts + usage back to the gateway.",
  "scripts": {
    "dev": "tsx src/main.ts dev",
    "start": "tsx src/main.ts start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@livekit/agents": "^1.4.7",
    "@livekit/noise-cancellation-node": "^0.1.10",
    "dotenv": "^16.4.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
